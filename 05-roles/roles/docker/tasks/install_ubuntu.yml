---
- name: Update apt cache
  apt:
    update_cache: true
    cache_valid_time: 3600
  become: yes

- name: Install Docker (Debian/Ubuntu)
  apt:
    name: docker.io
    state: present
  become: yes
